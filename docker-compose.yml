version: "3.8"

services:
  eternal-blossoms:
    build: .
    container_name: eternal-blossoms
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
    restart: unless-stopped
